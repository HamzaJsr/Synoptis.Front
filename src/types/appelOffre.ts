
export interface AppelOffreShortDTO {
  id: string;
  titre: string;
  description: string;
  dateLimite: string;  // ISO date string
  nomClient: string;
  statut: string;
  creeLe: string;      // ISO date string
}
